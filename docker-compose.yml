services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./ops/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - certpulse

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    depends_on:
      - prometheus

  certpulse:
    image: node:20-bookworm-slim
    working_dir: /app
    volumes:
      - ./:/app
      - ./data:/app/data
    command: sh -c "npm ci && npm run dev"
    environment:
      - PORT=3000
      - CERTPULSE_THRESHOLD_DAYS=14
      - DB_PATH=/app/data/certpulse.db
    ports:
      - "3000:3000"
